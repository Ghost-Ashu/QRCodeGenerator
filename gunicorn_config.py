bind = '0.0.0.0:8000'  # Change the port and host as needed
workers = 4  # Number of worker processes

# If you're using a virtual environment, specify its path
# pythonpath = '/path/to/your/virtual/environment'
